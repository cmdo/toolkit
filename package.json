{
  "name": "cmdo-toolkit",
  "description": "A collection of tools and services used to build software applications.",
  "private": true,
  "author": {
    "name": "Christoffer Rødvik",
    "email": "dev@kodemon.net"
  },
  "repository": {
    "url": "https://github.com/cmdo/toolkit.git"
  },
  "scripts": {
    "clean": "yarn --cwd ./packages/access clean && yarn --cwd ./packages/domain clean && yarn --cwd ./packages/http clean && yarn --cwd ./packages/inverse clean && yarn --cwd ./packages/router clean && yarn --cwd ./packages/socket clean",
    "api": "yarn --cwd ./api dev",
    "app": "yarn --cwd ./app dev"
  },
  "devDependencies": {
    "@types/jest": "26.0.22",
    "@types/ws": "7.4.0",
    "@typescript-eslint/eslint-plugin": "4.19.0",
    "@typescript-eslint/parser": "4.19.0",
    "eslint": "7.22.0",
    "eslint-config-prettier": "8.1.0",
    "eslint-plugin-prettier": "3.3.1",
    "eslint-plugin-simple-import-sort": "7.0.0",
    "jest": "26.6.3",
    "prettier": "2.2.1",
    "ts-jest": "26.5.4",
    "typescript": "4.2.3"
  }
}
