{
  "name": "cmdo-socket",
  "version": "1.1.0",
  "description": "Simple web socket solution written for nodejs.",
  "repository": "https://github.com/cmdo/toolkit.git",
  "author": "Christoffer Rødvik <christoffer@kodemon.net>",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [
    "server",
    "socket"
  ],
  "cmdo": {
    "type": "module"
  },
  "scripts": {
    "prepublishOnly": "yarn build",
    "build": "yarn clean && yarn && yarn lint && yarn compile",
    "clean": "rm -rf ./dist ./node_modules ./tsconfig.tsbuildinfo",
    "compile": "../../node_modules/.bin/tsc -b",
    "watch": "../../node_modules/.bin/tsc -w",
    "test": "node --inspect ../../node_modules/.bin/jest --runInBand --verbose",
    "lint": "../../node_modules/.bin/eslint 'src/**/*.ts'"
  },
  "dependencies": {
    "ws": "7.4.2"
  }
}
